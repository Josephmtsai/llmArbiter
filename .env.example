# Arbiter Dashboard — environment variables
# Copy to .env and fill in values (never commit .env)

# Server-side only — UI login password (maps to runtimeConfig.authPassword)
NUXT_AUTH_PASSWORD=change-me-at-least-32-characters-long

# nuxt-auth-utils session encryption key (generate with: openssl rand -hex 32)
NUXT_SESSION_PASSWORD=change-me-at-least-32-characters-long

# External Arbiter API
NUXT_PUBLIC_API_BASE=https://artbiter-production.up.railway.app
NUXT_PUBLIC_API_KEY=your_api_key_here

# Server-side Arbiter proxy for optimizer workflow.
# Browser calls /api/arbiter/*; Nuxt forwards X-API-Key server-side.
NUXT_API_BASE_URL=https://artbiter-production.up.railway.app
NUXT_API_KEY=your_api_key_here

# Railway/Nitro server port
PORT=8080
